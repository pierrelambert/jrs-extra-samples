You can place here unix scripts which will be automatically executed when the VM's configuration changes (RAM, CPU, IP) after the services start. They can change the data in databases, the objects in repositories...
